<%= form_for(@genres_movie) do |f| %>
  <% if @genres_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genres_movie.errors.count, "error") %> prohibited this genres_movie from being saved:</h2>

      <ul>
      <% @genres_movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %><br>
    <%= f.number_field :movie_id %>
  </div>
  <div class="field">
    <%= f.label :genre_id %><br>
    <%= f.number_field :genre_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
